box: wercker/android@2.0.0

build:
  step:
    - script:
        name: show base information
        code: |
          gradle -v
          echo $ANDROID_HOME
          echo $ANDROID_BUILD_TOOLS
          echo $ANDROID_UPDATE_FILTER
    - script:
      name: chmod gradle
      code: chmod +x gradlew
    - script:
      name: build gradle
      code: ./gradlew build test
